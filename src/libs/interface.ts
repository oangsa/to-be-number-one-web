export interface studentData {
    name: string,
    surname: string,
    studentId: number,
    yearClass: number,
    class: number,
    reason: string,
    total: number,
    oldMonth: number,
    timestamps: Date
}

export interface loginData {
    username: string,
    password: string
}